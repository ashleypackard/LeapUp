<%= content_for(:title, "About") %>
<h1><%= "#{@account.first_name} #{@account.last_name}" %></h1>
<h5><%= "#{@account.email}" %></h5>
<div class="row">
	<div class="col-md-6">
		<h3>Bio</h3>
		<p>
		<% if @account.description.blank? %>
			Why don't you add a bio in the Account Settings page!
		<% else %>
			<%= @account.description %>
		<% end %>
		</p>
	</div>

	<div class="col-md-6">
		<h3>Interests</h3>
		<ul>
			<li>Pool</li>
			<li>Running</li>
			<li>Frisbee</li>
		</ul>
	</div>
</div>