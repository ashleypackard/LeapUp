<nav class="navbar navbar-default navbar-static-top navbar-inverse">
  <div class="container">
  	<span class="navbar-brand" id="brand">LeapUp</span>
  	<ul class="nav navbar-nav">
      <li class="active">
        <%= link_to home_path do %>
          <i class="glyphicon glyphicon-home"></i>
          <span>Home</span>
        <% end %>
      </li>
      <li><a href="#"><i class="glyphicon glyphicon-eye-open"></i> View Feed</a></li>
      <li><a href="#"><i class="glyphicon glyphicon-pencil"></i> Make Post</a></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= "#{@current_account.first_name} #{@current_account.last_name}" %> <span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
          <li>
            <%= link_to account_path(@current_account.id) do %>
              <i class="glyphicon glyphicon-user"></i>
              <span>Profile</span>
            <% end %>
          <li>      
            <%= link_to edit_account_path(@current_account.id) do %>
              <i class="glyphicon glyphicon-cog"></i>
              <span>Settings</span>
            <% end %>
          </li>
          <li>
            <%= link_to logout_path, method: "delete" do %>
              <i class="glyphicon glyphicon-log-out"></i>
              <span>Logout</span>
            <% end %>
          </li>
        </ul>
      </li>
    </ul>
  </div>
</nav>