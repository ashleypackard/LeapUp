<%= content_for(:title, "Leave Comment") %>
<div class="row">
	<div class="form-group col-md-6 col-md-offset-1 side-bar-right">
		<%= render :partial =>"posts/post", locals: {post: @post} %>
		<% if !@comments.nil? %>
			<%= render @comments %>
		<% end %>
	</div>

	<div class="form-group col-md-3 col-md-offset-1">
	<h3>Leave a comment...</h3>
	<%= form_for [@post, @comment], html: {id: "new_comment_form"} do |f| %>
	<% if @comment.errors.any? %>
				    <div id="error_explanation">
				      <h2>
				        <%= pluralize(@comment.errors.count, "error") %> prohibited
				        this comment from being saved:
				      </h2>
				      <ul>
				        <% @comment.errors.full_messages.each do |msg| %>
				          <li><%= msg %></li>
				        <% end %>
				      </ul>
				    </div>
				    <% end %>
	<%= f.text_area :body, required: true, class: 'form-control' %> 
	<%= f.submit class: "btn btn-default btn btn-primary" %>


		<% end %>
	</div>
</div>
